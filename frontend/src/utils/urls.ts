const apiUrl = process.env.NEXT_PUBLIC_DB;

export const basicUrl = `${apiUrl}`;
export const auth = `${apiUrl}/auth`;
export const answers = `${apiUrl}/answers`;
export const questions = `${apiUrl}/questions`;
